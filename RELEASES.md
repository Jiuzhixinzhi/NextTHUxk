# NextTHUxk 发布说明

<!-- 发布前人工定稿：本文件是草稿，最终以 Release 说明为准（从本文件导出并润色）。 -->

## v3.5.0

> **架构深化 + 缺陷修复（无新功能）**：把散落的重复口径收拢为单接缝（课程池 / 回填 / 志愿解析 / 弹窗 / 单课动作），session 666→320 行；修复工作台打不开、前导零课班丢类型、提交假失败、退队双提交、志愿检查点不刷新、学分模拟漏课等缺陷；周次感知冲突（1-8 周与 9-16 周、单双周不再误报）。

### 缺陷修复（用户可见）

- **工作台打不开（线上热修）**：storage 中 plan/manualEvents/drafts 为真值非数组时绕过版本守卫，`(n||[]).forEach` 抛错致白屏；读入即归一 + 清毒自愈（`store.getArray`）
- **周次感知冲突**：1-8 周与 9-16 周（及单双周）不再误报；外校课文字说明钟点进入冲突检测与课表预览、自定义占用徽章重新可见
- **前导零课班丢课程类型**：`resolveCourseZy` 用原始课序拼键查归一后的 levelMap（'01' vs '1'）
- **提交/退选「假失败」**：写确认轮询裸串比较课序，把已生效的操作报成未生效
- **退选/退队双提交窗口**：候选队列与课表预览缺在途互斥，连点叠出第二个确认弹窗并悬挂首个 promise
- **志愿检查点刷新名存实亡（v3 改写期取反）**：恢复 v1.5.0「过期则拉」——窗口翻转后重拉过期院系，同一窗口内不再重复拉取
- **学分模拟静默漏课**：`CreditSimModal`/`DraftCourseRow` 裸串比较课序取不到池行，概率不计入模拟
- **课表块键缺 day 维度**：外校课复合日（如「周二、周四 15:00-16:30」）丢块
- v3 改写期回归：`zyCache` 只写不读恢复水合、志愿号补问弹窗恢复可达、换学期重置回填预算

### 架构深化（模块 / 接缝）

- session 666→320 行：拆出 `stores/launch.svelte.ts`（启动编排根）+ `domain/backfill.ts` / `stores/backfill.svelte.ts`（回填引擎合一）+ `domain/zy.ts` / `stores/zy.svelte.ts`（志愿号解析）
- `domain/pool.ts`：课程池合并单接缝（消除 4 套复制粘贴）
- 课班复合键全库统一 `keyOf`（删除 3 个拼写克隆）；`domain/card-model.ts` 视图模型（课卡 533→381 行）
- `ModalHost` 按类型拆为 5 个子组件（409→129 行）；单课动作 `dropCourseFlow`；双解码判据收拢 `pickDecodedWithSource`

### 测试与决策

- vitest 174 → **264 例**（pool / card-model / backfill / zy / decode / paged / preview / gbk-table / update-check / volunteers 等守门测试）
- `docs/adr/0001-0005`：周次冲突 / 兼容底线 / reviews fail-soft / 新鲜度谓词命名 / 防御读自愈豁免 DATA_VER

### 行为保真不变量（未改变）

风暴护栏数值 · 一次性 token 链 + pollUntil 不假成功 · GBK 双向契约 · reviews fail-soft + CC BY-NC · WebVPN 前缀 + AES-CBC（key 勿改）· 检查点 8/12/16/20 志愿窗口缓存 · DATA_VER=7

## v3.4.0

> **优先任选通道 + 队列暂存 + 上游 v2.1.0–v2.2.1 同步**：概率链新增「优先 N%」独立档，候补行可暂存进草稿，启动先出已选、差分提交改「先选后退」不丢课，解析/分页/会话健壮性全面对齐上游。

### 优先任选档（rx）

- 概率网格新增「优先 N%」独立档：志愿串 `(N)` 前缀 = 优先任选通道（`is_zyrxk=1`），可争位 = 本批池 − 必修三档 − 限选三档，概率 = 位/优先人数
- 新增 `domain` 优先档概率卷积 + 概率趋势历史优先档（`priAt`/`priSeries`/`priDelta`，随窗口重算）
- `ProbTrendModal` 档位新增「优先」tab（`selZy=0`）；`CourseCard` 级联链渲染优先格（label 优先 / key rxp），点击进趋势

### 队列暂存与启动先出已选

- 候补队列行新增「**暂存**」按钮（类型/志愿取候补行真值），候补条目并入活跃草稿
- 草稿行显示已选/排队徽章（派生自池行状态）
- 启动改两段式：**已选先到先上屏**，候补/培养方案/等级/分类属性到货后增量并入

### 提交可靠性

- **promote 差分提交改「先选后退」**：最坏回提交前原状可重试（旧版先退后选中断即永久丢课）；失败文案标注已选/已退进度
- **退课确认成功即本地摘牌** + 后台 `refreshSelected` 兜底
- 排队中课程不参与差量提交（草稿条目 `queued` 标记；退队走候选队列/卡片）
- 导出复制可用性探测 + `execCommand` 返回值检查 + 手动复制弹窗兜底（旧版两路全败仍假成功）
- `closeModal` 结算悬挂 promise（dialog→false / prompt→null / manualCopy→false）

### 解析与数据正确性（上游 v2.1.0–v2.2.1 同步）

- kyl 搜索仅取第 1 页 + 单课号上限 10 页 + 按课号归一合并，修复形策一班一师漏后半教师
- 候补 `parseDl` 无码型校验（表头行被当数据的幽灵行根修）+ 体育候补 typeCode 补 `ty`
- 学分末位规则 `creditsOf`（纯数字课号恒取末位 / 外校前缀回退表内值），接入目录/页签/已选/候补/培养方案
- 已选表教师列纯数字拒绝留空（防学分数字串误入）
- `mergeRows` 刷新池行容量/余量

### 查询与匹配健壮性（上游 v2.1.0–v2.2.1 同步）

- SSO 统一认证登录页独立判定（`pageKind='sso'`）：明确「重新登录 WebVPN / 校园网内可直接 jwweb」；不再白跑换票
- **kkxxSearch 会话游标闸门**：总线加前台占用计数 + 启动落定标记，前台查询/加载全部/跳转补爬进出计数，后台补拉与浏览扫页逐页让路（防污染服务端会话游标）
- `serverSig` 死守卫修复（查询落定写回真实指纹）；查询签名变化先裸发一次 `kkxxSearch` 重建会话；浏览分页错配检测/回滚
- 池行三段匹配（新增 `domain/match.ts`：归一精确课序 → 同课同师 → 首行兜底），借源/时间回填/候补元数据三处统一
- 培养方案跳转课号粒度（`seq='0'`）：该课任一课序即达，不再白爬全量
- storm 失败判定对齐上游：0 行或 0 新键均计入失败重试；深页精确课号前缀过滤防未筛选行污染池

### 课表与卡片交互

- 占用覆盖层 z-index 提升（压过课程 hover），底部灰粗体「名称+时间」标签，仅行内 ✕ 拦截点击、其余穿透；窄道不换行、✕ 常驻可见
- 课程卡自动折叠优先级：已选/候补 > 冲突折叠 > 开课线风险/可用展开（冲突压过未达开课线）

## v3.3.0

> **已选课表直接学分模拟 + 自定义占用上表 + 课程卡渐进式披露**：占用块进课表网格、满员/冲突课卡折叠一行式、校评徽章全量缓存、备份 v3 概率缓存随行，并移植上游 v2.0.1/v2.1.0 关键修复。

### 课程卡渐进式披露

- 已满/冲突课卡折叠一行式：红标明示冲突课节 + 关键数字 + 主按钮；可用/已选/候补/开课线风险自动展开
- 预选 9 格级联概率链收敛为操作行 xx% 加框按钮，按需展开

### 学分模拟

- **当前已选课表直接学分模拟**（含候补；课余量阶段正选锁定恒 1）
- 直方图最高柱概率小字被裁修复（柱高 92→84px 收进 110px 容器）
- 概率趋势折线动态 y 域：min/max ±padding 缩放凸出波动，域上下界标注 + 每点百分比标签（>12 点只标最近 12）

### 校评（教务评教分数）一次性缓存

- `cm=xgpg_qbkcmycdzbData` 全量拉取（学期静止数据，每学期一次），课程卡/联想并列校评徽章（7 分制，课号×教师粒度）

### 自定义占用上表

- 占用块与课程同池渲染进课表网格：自由钟点紫色「自定义」块（原仅 chip 列表），半透明斜纹底标识；与课程重叠时**不再挤占分道**，改为全宽覆盖层叠于课块上方（不拦截课程点击，✕ 常驻右下角可直接删除），多个占用互相重叠仍自动分道
- 占用参与全部冲突检测的口径不变（草稿硬阻断、卡片冲突提示、筛选 chip）

### 备份 v3（backupVer=3）

- 导出新增概率缓存：**概率趋势历史**（`probHist`，60 窗口快照）+ **当前检查点窗口志愿缓存**（`volCache`，volMap + 院系抓取时间戳），换机不再丢趋势、免重复拉取志愿
- 导入合并策略：趋势历史同学期按窗口点合并（按 t 去重 / 同窗替换 / 超限截断，结构坏条目跳过）；志愿缓存**仅同检查点窗口**才收（跨窗口陈旧数据明确提示跳过），`depts` 时间戳只进不退
- 兼容导入 v1/2/3；`stageCart`/草稿/占用的既有合并语义不变

### 上游移植（v2.0.1/v2.1.0 关键修复）

- **版本单源**：运行时版本由 `curVer()` 读 `chrome.runtime.getManifest().version`，constants 仅留 vitest 回退值 `VER_FALLBACK`（上游 v2.0.1 实录：双版本源漂移致永远提示更新）；CI 守门改为 tag/manifest 一致性
- **WebVPN 壳页自愈**：http 层识别壳页后 `setWebvpnReenter` 自动重进
- **跳转教师仲裁**：跳转定位的教师字段仲裁（搜索 store）

### 缺陷修复

- **自定义占用无法持久化（根因修复）**：`addManualEvent`/`removeManualEvent` 直传 Svelte 5 `$state` Proxy 给 `chrome.storage`，序列化失败被静默吞掉——当次会话看似成功、重启即丢；现与全库惯例一致先深拷贝落盘，失败时明确 toast 提示
- **重叠课块分道宽度错乱（v2 二次遍历在 v3.0 重构时丢失）**：同日簇内首块分配 lane 时 `ends` 尚未收满，`lanes` 少记一档——首块以全宽压住后续分道；现恢复 v2 `render.js` 的二次遍历统一簇内 `lanes`（测试实锤）
- **搜索结果行徽章丢失**：`mergeRows` 改为每次合并无条件补挂校评/社区评价
- **培养方案「等待加载」+ 返回原系统再开启概率丢失**（用户报）

## v3.2.0

> **Svelte 5 全面重构落地 + 概率趋势历史 + 课程卡直接正选恢复**：原 vanilla JS + `NX.*` 命名空间一次性退役（v2.3.0 → v3.2.0 单发），叠加检查点概率走势回看与未选课班一键正选。

### 课程卡直接正选恢复

- 未选课班双按钮并存：「**选课**」主按钮（课余量阶段余量为 0 时切换为「**排队选课**」）+ 「加入草稿」次按钮（样式对齐已选分支的草稿按钮）
- 提交前 confirmDialog 确认：课名 / 课序 / 类型 / 志愿；排队态附「余量为 0，将进入候补队列」说明
- 复用现成 `submitCourse` 链路：一次性 token 链（课满二段 `saveBksKcDl` 自动入候补）+ REJECT_RE 拒绝字典 + pollUntil 轮询确认；结果走 `showXkResult` 真实反馈，随后整表刷新回填池/已选/候补
- v3.0.0 重构时预留的 `session.doSubmitCourse` 代理重新接线（域/api 层零改动）

### 概率趋势历史

- **窗口快照留档**：每次志愿数据到货（启动 / 检查点 8/12/16/20 同步 / 按需补拉）把 VolDatum 原始值按当前窗口存为一条点；同窗口后到数据覆盖修正（幂等）；保留最近 60 窗口（约 15 天），按学期隔离
- **卡片涨跌指示**：级联概率链当前选法旁显示相对上一有效窗口的 ▲/▼/– 百分点（绿升红降）
- **点击链节点看档位趋势**：级联链任意一格点击弹出概率趋势模态——该 (类型, 志愿) 档位的历史折线 + 窗口时间轴 + 当前比值，模态内可切换全部合法档位
- 仅存本地（`probHist` 键，sem + 窗口作用域），不触碰任何服务端；队列/补选阶段不记录；不递增 DATA_VER（新增键，无旧结构破坏）
- 实现：`domain/probhist.ts` 纯函数（快照/合并/伪课程重算/SVG 路径），挂点 `volCachePersist` 防抖内先快照、变化才写盘

### 大型重构（Svelte 5 + TS + Tailwind 4）

> 一次性大型重构（2026-09）：原 vanilla JS + `NX.*` 命名空间 + manifest 脚本注入顺序全部移除。

#### 架构（目标 1-3）

- **消除 god class**：`NX.state`（~40 字段）拆为按域 stores（session/search/drafts/volunteer/reviews/backup…），显式 import 依赖图；manifest 注入顺序不再是依赖契约
- **消除隐式时序依赖**：数据层不再直接调渲染函数（mergeServerRows/_volFetchNow 层间违规 10+ 处归零）；`volNeedsRefresh` 跨文件覆盖消亡（volunteer 单一实现）；`try{render()}catch` 保险（~15 处）自然消失
- **消除手动 HTML / 手动视图模型**：35 处 innerHTML 拼接 → Svelte 模板；`NX.esc` 退役；selVersion/poolVersion/invalidatePreview 缓存链 → `$derived`；预览模式「label 字符串回读」→ 显式 `previewTarget`；筛选/查询状态源不再是 DOM（`_serverSig`/query 对象全 store 化）；RENDER_CHUNK 渐进渲染退役（服务端分页 20 行/页）

#### 草稿模型（目标 5，用户定稿）

- **「暂存」概念移除**：草稿 = 可直接编辑的命名方案（至多 5 份），改动即时保存；无「保存草稿/载入暂存/导出暂存」步骤
- **活跃草稿**：唯一编辑目标；课程卡「加入草稿」并入活跃草稿（同课班去重），**不提交正选变更**，且**硬阻断新的时间冲突**（对活跃草稿 + 自定义占用区间检测，冲突时拒绝并提示课程/时段）
- 已选载入 / JSON 导入并入活跃草稿（不阻断冲突，面板/预览标红展示）；草稿预览/学分模拟/提交选课按草稿
- 卡片上保留的直接正选入口：退选 / 退队 / 志愿调整；其余变更走「提交选课」（差量：保留 X · 退 Y · 新选 Z）
- 备份 v2 = {drafts, manualEvents}；兼容导入 v1（stageCart 并入活跃草稿）
- 旧数据：DATA_VER=7 首启清理 stageCart/config/grade（升级直弃，用户定稿）

#### AI 移除（目标 6）

- 删除 AI 排课 / AI 课程搜索 / AI 配置；删除 `config`（API 配置）与 `grade`（年级，仅 AI 用）存储键；reviews 的 `_tbSnip` 一并删除；评分徽章/联想词/排序保留
- popup 标语 / manifest 描述去除 AI 字样

#### UI（目标 4）

- 液态玻璃配方迁入 Tailwind `@theme` 令牌 + 全局层；`#lg-refract` 折射滤镜单实例
- prompt()/confirm() 全部替换为玻璃模态框（重命名/新建/替换/提交确认/删除确认/学期设置）
- 全部内联样式清除；筛选栏/时间轴/联想下拉等组件化；分页条/数据不完整入口保留

#### 平台

- Firefox 兼容底线 115 → **128**（Tailwind 4 CSS 基线）
- 构建：Vite 8 + Rolldown，content 单 IIFE（MV3 无动态 import 限制）+ popup ESM；`pnpm build` 出 `dist/`（manifest + icons 拷贝）
- 质量门：`pnpm check`（svelte-check 0 error）+ `pnpm test`（vitest 全绿：时间/冲突/概率/GBK/差量/方案覆盖）；CI 增类型检查/单测步骤

### 行为保真不变量

GBK 双向契约 · 风暴护栏全规则 · 一次性 token 链 + pollUntil 不假成功 + REJECT_RE · reviews fail-soft + CC BY-NC · WebVPN 前缀 + AES-CBC（key 勿改）· 检查点 8/12/16/20 志愿窗口缓存 · knote/zyCache/外校钟点/培养方案特殊规则 · 差量提交语义。
