# NextTHUxk 发布说明草稿

> ⚠️ **草稿状态**：请亲自修改后再发布。以下素材按 git 历史整理，措辞与取舍以你为准。
> 2.0.1 为 9/5 用户报告驱动的补丁：首屏志愿概率缺失（竞态）+ 检查点窗口缓存。
> 2.1.0 为融合「学分蒙特卡洛模拟器」网页工具的学分中签模拟功能（精确 DP 取代随机抽样）。
> 2.2.0 为暂存区编排入口（已选 / 草稿载入）+ 志愿概率首屏竞态根治（PR #2 补刀：院系码回退 / p_kch 分批）。
> 2.3.0 为一键备份（暂存 / 草稿 / 占用统一导出导入智能合并）+ 提交选课差量对齐 + 跳转自动补齐 + 课表预览 hover 课序。

---

# OneTHU NextTHUxk 2.3.0 — 一键备份 + 差量提交 + 跳转自动补齐

**主题**：本地编排数据（暂存 / 草稿 / 占用）一键带走跨设备恢复；「提交选课」改为差量对齐（重合课不动）；右侧跳转目标落在未探测页时自动全量补齐。

### 一键备份

- 顶栏「备份」下拉菜单一键导出 / 导入 json：暂存课表、全部草稿、自定义占用一次带走（导出文件名带学期 + 时间戳），跨设备恢复编排现场
- 导入按课班 / 草稿名 / 占用键**智能合并**，绝不覆盖既有数据：暂存课班去重、草稿同名替换异名追加（满 5 份 prompt 编号替换）、占用按 名称+星期+起止 去重
- 备份文件 `app`+`backupVer` 双校验防误导入；学期不一致导入前 confirm；导入结果逐项汇报（+暂存 / 草稿新增 / 替换 / 跳过 / 占用）

### 提交选课差量对齐

- 「提交选课」不再先退全部重选：与当前已选重合的课（课序、志愿、类型一致）保持不动，只退多余、选新增；志愿 / 属性不一致或已选信息不全（兜底路径）按退 + 重选处理
- 确认弹窗展示精确计数（保留 X · 退 Y · 新选 Z）；两侧完全一致直接提示无需提交
- 每步校验 `res.ok` 失败即中断并如实汇报已执行步数（不假成功）；并发提交加 in-flight 守卫、空草稿拦截

### 跳转自动补齐

- 根因：精确课号按风暴护栏只探 1 页（20 行），教务 `p_kch` 却是包含匹配——100+ 行的课目标常落在未探测页，此前只剩「数据不完整」横幅死等
- 跳转目标不在已加载行且教务确有缺页（总页数 2-25）：自动 forceAll 全量补齐，命中后翻到目标页并高亮；页数 >25 不深探（风暴护栏），留手动「加载全部」
- 评审修正三连：`loadAllSearch` 过期写入竞态守卫（sig 快照不符整批丢弃、跳转接管退出）、`loadAll` 复用生效查询快照（kch 0 行课名兜底后方向延续）、`findRow` q 过滤与 `filterCourses` 对齐

### 课表预览 hover（用户报）

- 课块 tooltip 由「课名·大节序号」改为「课名·课序·天-大节(周次)」；外校钟点块标周段标记，手动占用去多余后缀

---

# OneTHU NextTHUxk 2.2.0 — 暂存区编排入口 + 志愿概率首屏根治

**主题**：补齐「正选 / 草稿 → 暂存区」的编排链路，并根治首屏志愿概率缺失的竞态（PR #2 未修复的补刀）。

### 暂存区编排入口

- 暂存区新增「**已选载入**」：当前已选整表按课序去重并入暂存区（「存当前选课」保留不动）；草稿卡新增「**载入暂存**」，任意草稿以现有课表为底稿继续编排
- 去重按课班复合键（`code + seq`、`normSeq` 归一），并入后暂存预览即时刷新
- 空态文案列全四入口，可发现性提升

### 志愿概率首屏竞态根治

- 根因：启动 / 暂存行无 `department` 字段，`fetchVolunteer` 院系集合全空 → 按需补拉永不触发，首屏概率缺失只能等点击带 `kkxx` 行才点亮
- 新增**课号中段院系码回退**（8 位纯数字课号取 2-4 位 + `DEPT_CODES` 白名单，体育课码回退加守卫）
- 右侧课表预览不随志愿到货重渲：`applyVolunteer` 变更计数驱动 `poolVersion++`，refresh / 收尾 / launch onDept 三处统一补 `renderPreviewTT`
- 定向 `p_kch` 补拉限 **4 门/次**（本批优先、池内旧行分批补）；缺行检查扩 `rows ∪ allCourses`；预算仍 3 次/课/院系
- `pagedFetch` EMPTY 页恢复轮按真末页吸收，消除 `vol-BR-024` STILL MISSING 日志噪音，全空页留空壳诊断

---

# OneTHU NextTHUxk 2.1.0 — 学分中签模拟

**新功能**：把「学分蒙特卡洛模拟器」融为扩展内建功能——暂存区 / 草稿的每门课本来就带实时中签概率（身份 × 志愿），无需手动粘贴 Excel。

- 暂存区与各草稿卡片新增「学分模拟」入口，弹窗展示整套方案的总学分概率分布
- 算法：各课中签相互独立 → 总学分分布可用动态规划卷积**精确计算**（取代原网页 10 万次蒙特卡洛随机抽样，无噪声、瞬时出结果）
- 指标：期望学分、最可能总学分、≥ 目标学分概率（默认取期望，可改）、参与统计门数
- 直方图与累计概率表为纯 CSS 柱条（不引 Chart.js），延续液态玻璃风格
- 无数据 / 课余量已满排队的课：弹窗内标红提示、可手动填概率覆盖
- **实时取值 / 手动覆盖双模式 tab**：实时分布与覆盖分布一键对比；被覆盖的课行程「改」角标（改回实时值即消失）——适合「名义难抢但同类课有余量」的课调高概率后再看总学分分布
- 依赖不变：无 CDN、无存储结构变更（`DATA_VER` 不动）

---

## OneTHU NextTHUxk 2.0.1

**2026 年 9 月 5 日用户实锤**：每次进入界面首屏只有少数课程显示「选上概率」，需要在课程上多点几次才会逐步点亮，且个别课程无法稳定复现；用户同时建议相关数据在同一教务周期（如 9/5 16:00~20:00）采集缓存，避免后端重复请求。

### 首屏概率缺失（竞态根因实锤）

- v2.0 随时查询架构下，启动的志愿统计抓取排在课余量探测（多请求、秒级）之后才进入，首屏浏览页行先经搜索管线到达时，按需补拉被 `_volDepts` 未初始化门控整段跳过；启动路径也只把数据套到池行（已选/候补），不写搜索行对象——首屏行于是永远停在「无数据」，直到后续搜索把行重新合入
- 修复：去掉门控（按院系检查点窗口新鲜度过滤）、启动拉完立即补套搜索行、补拉回调加 busy-guard 并**每院系到货增量回渲**
- 院系抓取 in-flight 去重：同一院系并发只请求一次（旧实现 launch/补拉叠跑重复请求）
- 缺行自愈墓碑改 3 次/会话预算：一次瞬态失败不再终身拉黑（「怎么点都不出」的来源）

### 检查点窗口缓存（用户建议采纳）

- 志愿统计持久化（`volCache`：学期 + 窗口起点 + 行数据）——同一检查点窗口（8/12/16/20）内重进界面零后端请求、概率秒出
- `volNeedsRefresh` 检查点判定从死码转正：已拉院系按窗口计新鲜度，翻转才过期；检查点自动同步真正刷新志愿数据并写回缓存（顶栏提示名实相符）
- 提交选课/退课不再强制全池重拉志愿统计（上游只在检查点发布——纯缓存套用；课余量/候补仍实时）

---

# NextTHUxk 2.0「随时查询」— 发布说明草稿

> ⚠️ **草稿状态**：请亲自修改后再发布。以下为按 git 历史（1.5.0 → 2.0，约 73 个提交）整理的素材，措辞与取舍以你为准。

---

## OneTHU NextTHUxk 2.0.0

**从 2026 年 3 月迭代到 2026 年 9 月，我们于今天隆重发布 OneTHU NextTHUxk 扩展 2.0.0 版本。**

相较于 1.x 版本，2.0 版本进行了**革命性**的架构更新，并且修复了大量潜在的恶性 bug，也是 OneTHU 平台的统一调度架构首次实验。NextTHUxk 将作为 OneTHU 平台的首个官方插件推出。

### 架构更新

在 1.x 版本中，首次打开扩展需要调用教务系统 API 进行全校 6,000 余门课程的信息与志愿数据的全量爬去，丢包、缺漏现象极多并且耗时极长，后续的缓存命中时间也令人无法接受。

在 2.0.0 版本中，**NextTHUxk 不再会对所有内容进行一次性全量爬取，而是采取更合理的随查随取。我们将根据用户的查询与选课情况随时调取教务系统 API，随时解析。实测工作台启动速时间从 50s+ 提升到 5s 以内，工作台就绪时间不超过 10s。**

由于不需要命中缓存，**工作台的浏览器内存占用将缩小一半以上，占用扩展存储将下降到原来的 1/100 。**

### 课程库解析更新

得益于架构更新，我们不再一次性爬取全部教务系统信息页，因此不再受到课程遗漏丢包的困扰。配合新加入的三次重试机制，截至目前，**2026 秋季学期本科生可选的 6,176 门课程全部可以在 NextTHUxk 进行查询，包括北京大学、北京外国语大学开放的互选课程。**

我们同时**完全重构了预览课表机制**。由于北京大学、北京外国语大学课程时间与清华大学课程时间无法完全匹配，我们直接将课表预览更新为**动态时间轴**，并且**完全支持外校互选课程的所有时间解析功能**。你可以在时间轴上自由添加占用、本校课程或外校互选课程，更清晰的预览内容。

我们修复了书院专项体育、航空体育、体育理论等**非全校统一体育课程**的解析错误问题。

### 交互更新

根据社区开发者的 PR，现在你可以直接在预览课表/暂存区/培养方案处点击任意课程条，即可跳转至该课程以及同课号下所有的课程，方便您筛选。

左边筛选栏现在可以收起，方便即使查看，减小视线遮挡。

### AI 辅助选课更新

我们依据全新架构更新了 AI 辅助选课接口。现在 AI 可以更准更快的提供参考了。**需要注意的是，由于选课决策本身是很复杂的规划问题，可能绝大多数大语言模型需要极长的时间推出建议。**

### 品牌更新

NextTHUxk 现在是 OneTHU 平台的官方插件部分，我们为其更换了 OneTHU 的品牌 Logo 与识别符号。未来会基于 OneTHU 平台推出更多更新，欢迎贡献，敬请期待。

### 致谢
- [THUxk](https://github.com/orgs/THUxk) 团队的长期社区贡献与建议
- 清华大学 2026 级新生群的使用建议

---

> 再次提醒：本文件为**草稿**，发布前请全文过目修改。README.md 已同步 2.0 内容（含 v2.0 历史节），同样等你定稿。
